VITE_UPLOAD_BASE=https://pos.itnvend.com:4000
# Dev proxy target for Vite: forwards /api requests to the running POS backend
# Set both proxy target and API base so the client uses the correct backend.
VITE_API_PROXY_TARGET=https://pos.itnvend.com:4000
VITE_API_BASE=https://pos.itnvend.com:4000
